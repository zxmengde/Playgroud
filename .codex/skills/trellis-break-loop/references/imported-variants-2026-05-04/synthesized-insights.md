# Synthesized Imported Variant Insights

This file is synthesized from full reads of disabled overlapping skills. Treat these notes as active guidance for the keeper skill, not as an archive. Read the source folders only when this synthesis is insufficient.

Keeper: trellis-packages-cli-src-templates-codex-skills-break-loop

## How To Apply

- Prefer the keeper skill workflow as the default path.
- Add the trigger phrases, verification checks, output contracts, and resource pointers below when they fit the task.
- If a disabled variant contains scripts, templates, references, examples, or assets, treat them as reusable resources from this keeper skill.
- Do not resurrect a disabled variant as a separate active skill unless the keeper cannot express its behavior cleanly.

## Source: trellis-.agents-skills-trellis-break-loop

Trigger/description delta: Deep bug analysis to break the fix-forget-repeat cycle. Analyzes root cause category, why fixes failed, prevention mechanisms, and captures knowledge into specs. Use after fixing a bug to prevent the same class of bugs.
Actionable imported checks:
- **Tool Limitation**: grep missed it, type check wasn't strict
- [ ] Update check guidelines if needed
- **Sync templates** - After updating `.trellis/spec/`, sync to `src/templates/markdown/spec/`
- **Commit the spec updates** - This is the primary output, not just the analysis text

## Source: trellis-.claude-skills-trellis-break-loop

Trigger/description delta: Deep bug analysis to break the fix-forget-repeat cycle. Analyzes root cause category, why fixes failed, prevention mechanisms, and captures knowledge into specs. Use after fixing a bug to prevent the same class of bugs.
Actionable imported checks:
- **Tool Limitation**: grep missed it, type check wasn't strict
- [ ] Update check guidelines if needed
- **Sync templates** - After updating `.trellis/spec/`, sync to `src/templates/markdown/spec/`
- **Commit the spec updates** - This is the primary output, not just the analysis text

## Source: trellis-.cursor-skills-trellis-break-loop

Trigger/description delta: Deep bug analysis to break the fix-forget-repeat cycle. Analyzes root cause category, why fixes failed, prevention mechanisms, and captures knowledge into specs. Use after fixing a bug to prevent the same class of bugs.
Actionable imported checks:
- **Tool Limitation**: grep missed it, type check wasn't strict
- [ ] Update check guidelines if needed
- **Sync templates** - After updating `.trellis/spec/`, sync to `src/templates/markdown/spec/`
- **Commit the spec updates** - This is the primary output, not just the analysis text

## Source: trellis-.opencode-skills-trellis-break-loop

Trigger/description delta: Deep bug analysis to break the fix-forget-repeat cycle. Analyzes root cause category, why fixes failed, prevention mechanisms, and captures knowledge into specs. Use after fixing a bug to prevent the same class of bugs.
Actionable imported checks:
- **Tool Limitation**: grep missed it, type check wasn't strict
- [ ] Update check guidelines if needed
- **Sync templates** - After updating `.trellis/spec/`, sync to `src/templates/markdown/spec/`
- **Commit the spec updates** - This is the primary output, not just the analysis text

## Source: trellis-.pi-skills-trellis-break-loop

Trigger/description delta: Deep bug analysis to break the fix-forget-repeat cycle. Analyzes root cause category, why fixes failed, prevention mechanisms, and captures knowledge into specs. Use after fixing a bug to prevent the same class of bugs.
Actionable imported checks:
- **Tool Limitation**: grep missed it, type check wasn't strict
- [ ] Update check guidelines if needed
- **Sync templates** - After updating `.trellis/spec/`, sync to `src/templates/markdown/spec/`
- **Commit the spec updates** - This is the primary output, not just the analysis text
