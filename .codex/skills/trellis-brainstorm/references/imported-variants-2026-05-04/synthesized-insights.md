# Synthesized Imported Variant Insights

This file is synthesized from full reads of disabled overlapping skills. Treat these notes as active guidance for the keeper skill, not as an archive. Read the source folders only when this synthesis is insufficient.

Keeper: trellis-packages-cli-src-templates-codex-skills-brainstorm

## How To Apply

- Prefer the keeper skill workflow as the default path.
- Add the trigger phrases, verification checks, output contracts, and resource pointers below when they fit the task.
- If a disabled variant contains scripts, templates, references, examples, or assets, treat them as reusable resources from this keeper skill.
- Do not resurrect a disabled variant as a separate active skill unless the keeper cannot express its behavior cleanly.

## Source: trellis-.agents-skills-trellis-brainstorm

Trigger/description delta: Guides collaborative requirements discovery before implementation. Creates task directory, seeds PRD, asks high-value questions one at a time, researches technical choices, and converges on MVP scope. Use when requirements are unclear, there are multiple valid approaches, or the user describes a new feature or complex task.
Unique headings to preserve:
- Delegate to `trellis-research` sub-agent (don't research inline)
- Research steps (to pass into each sub-agent prompt)
- Research References
Actionable imported checks:
- **Action-before-asking** (reduce low-value questions)
- **Research-first** for technical choices (avoid asking users to invent options)
- **Action before asking**
- Check configs, scripts, existing command definitions
- The sub-agent has its own context window → doesn't pollute brainstorm context with raw tool output
- After each user answer:
- Update `Acceptance Criteria` with testable checkboxes
- **Preference decisions** (after presenting concrete options)
- Asking user to choose an approach before presenting concrete options

## Source: trellis-.claude-skills-trellis-brainstorm

Trigger/description delta: Guides collaborative requirements discovery before implementation. Creates task directory, seeds PRD, asks high-value questions one at a time, researches technical choices, and converges on MVP scope. Use when requirements are unclear, there are multiple valid approaches, or the user describes a new feature or complex task.
Unique headings to preserve:
- Delegate to `trellis-research` sub-agent (don't research inline)
- Research steps (to pass into each sub-agent prompt)
- Research References
Actionable imported checks:
- **Action-before-asking** (reduce low-value questions)
- **Research-first** for technical choices (avoid asking users to invent options)
- **Action before asking**
- Check configs, scripts, existing command definitions
- The sub-agent has its own context window → doesn't pollute brainstorm context with raw tool output
- After each user answer:
- Update `Acceptance Criteria` with testable checkboxes
- **Preference decisions** (after presenting concrete options)
- Asking user to choose an approach before presenting concrete options

## Source: trellis-.cursor-skills-trellis-brainstorm

Trigger/description delta: Guides collaborative requirements discovery before implementation. Creates task directory, seeds PRD, asks high-value questions one at a time, researches technical choices, and converges on MVP scope. Use when requirements are unclear, there are multiple valid approaches, or the user describes a new feature or complex task.
Unique headings to preserve:
- Delegate to `trellis-research` sub-agent (don't research inline)
- Research steps (to pass into each sub-agent prompt)
- Research References
Actionable imported checks:
- **Action-before-asking** (reduce low-value questions)
- **Research-first** for technical choices (avoid asking users to invent options)
- **Action before asking**
- Check configs, scripts, existing command definitions
- The sub-agent has its own context window → doesn't pollute brainstorm context with raw tool output
- After each user answer:
- Update `Acceptance Criteria` with testable checkboxes
- **Preference decisions** (after presenting concrete options)
- Asking user to choose an approach before presenting concrete options

## Source: trellis-.opencode-skills-trellis-brainstorm

Trigger/description delta: Guides collaborative requirements discovery before implementation. Creates task directory, seeds PRD, asks high-value questions one at a time, researches technical choices, and converges on MVP scope. Use when requirements are unclear, there are multiple valid approaches, or the user describes a new feature or complex task.
Unique headings to preserve:
- Delegate to `trellis-research` sub-agent (don't research inline)
- Research steps (to pass into each sub-agent prompt)
- Research References
Actionable imported checks:
- **Action-before-asking** (reduce low-value questions)
- **Research-first** for technical choices (avoid asking users to invent options)
- **Action before asking**
- Check configs, scripts, existing command definitions
- The sub-agent has its own context window → doesn't pollute brainstorm context with raw tool output
- After each user answer:
- Update `Acceptance Criteria` with testable checkboxes
- **Preference decisions** (after presenting concrete options)
- Asking user to choose an approach before presenting concrete options

## Source: trellis-.pi-skills-trellis-brainstorm

Trigger/description delta: Guides collaborative requirements discovery before implementation. Creates task directory, seeds PRD, asks high-value questions one at a time, researches technical choices, and converges on MVP scope. Use when requirements are unclear, there are multiple valid approaches, or the user describes a new feature or complex task.
Unique headings to preserve:
- Delegate to `trellis-research` sub-agent (don't research inline)
- Research steps (to pass into each sub-agent prompt)
- Research References
Actionable imported checks:
- **Action-before-asking** (reduce low-value questions)
- **Research-first** for technical choices (avoid asking users to invent options)
- **Action before asking**
- Check configs, scripts, existing command definitions
- The sub-agent has its own context window → doesn't pollute brainstorm context with raw tool output
- After each user answer:
- Update `Acceptance Criteria` with testable checkboxes
- **Preference decisions** (after presenting concrete options)
- Asking user to choose an approach before presenting concrete options
