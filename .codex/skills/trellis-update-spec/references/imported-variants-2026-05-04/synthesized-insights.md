# Synthesized Imported Variant Insights

This file is synthesized from full reads of disabled overlapping skills. Treat these notes as active guidance for the keeper skill, not as an archive. Read the source folders only when this synthesis is insufficient.

Keeper: trellis-packages-cli-src-templates-codex-skills-update-spec

## How To Apply

- Prefer the keeper skill workflow as the default path.
- Add the trigger phrases, verification checks, output contracts, and resource pointers below when they fit the task.
- If a disabled variant contains scripts, templates, references, examples, or assets, treat them as reusable resources from this keeper skill.
- Do not resurrect a disabled variant as a separate active skill unless the keeper cannot express its behavior cleanly.

## Source: trellis-.agents-skills-trellis-update-spec

Trigger/description delta: Captures executable contracts and coding conventions into .trellis/spec/ documents. Use when learning something valuable from debugging, implementing, or discussion that should be preserved for future sessions.
Actionable imported checks:
- "This is **what to consider** before writing" → Put in `guides/`
- Avoid duplicating content
- To avoid repeating mistakes → Yes, update spec
- `$finish-work` - Reminds you to check if specs need updates

## Source: trellis-.claude-skills-trellis-update-spec

Trigger/description delta: Captures executable contracts and coding conventions into .trellis/spec/ documents. Use when learning something valuable from debugging, implementing, or discussion that should be preserved for future sessions.
Actionable imported checks:
- "This is **what to consider** before writing" → Put in `guides/`
- Avoid duplicating content
- To avoid repeating mistakes → Yes, update spec
- `/trellis:finish-work` - Reminds you to check if specs need updates

## Source: trellis-.cursor-skills-trellis-update-spec

Trigger/description delta: Captures executable contracts and coding conventions into .trellis/spec/ documents. Use when learning something valuable from debugging, implementing, or discussion that should be preserved for future sessions.
Actionable imported checks:
- "This is **what to consider** before writing" → Put in `guides/`
- Avoid duplicating content
- To avoid repeating mistakes → Yes, update spec
- `/trellis-finish-work` - Reminds you to check if specs need updates

## Source: trellis-.opencode-skills-trellis-update-spec

Trigger/description delta: Captures executable contracts and coding conventions into .trellis/spec/ documents. Use when learning something valuable from debugging, implementing, or discussion that should be preserved for future sessions.
Actionable imported checks:
- "This is **what to consider** before writing" → Put in `guides/`
- Avoid duplicating content
- To avoid repeating mistakes → Yes, update spec
- `/trellis:finish-work` - Reminds you to check if specs need updates

## Source: trellis-.pi-skills-trellis-update-spec

Trigger/description delta: Captures executable contracts and coding conventions into .trellis/spec/ documents. Use when learning something valuable from debugging, implementing, or discussion that should be preserved for future sessions.
Actionable imported checks:
- "This is **what to consider** before writing" → Put in `guides/`
- Avoid duplicating content
- To avoid repeating mistakes → Yes, update spec
- `/trellis-finish-work` - Reminds you to check if specs need updates
